# Apply Progress: SDD Review JSON Contract

## Slices

### Slice 1: `catalog-schema-json`

- Boundary: Implement Phase 1 only: canonical static catalog JSON, per-change report schema JSON, and derived/reference-only semantics for `control-catalog.md`.
- Delivery: `auto-chain`, `stacked-to-main`, review budget 400 changed lines.
- Mode: Standard. Strict TDD is disabled and no executable test runner is available.

### Slice 2: `review-skill-markdown-rendering`

- Boundary: Implement Phase 2 only: update `sdd-review/SKILL.md` and `report-template.md` so `sdd-review` loads controls from JSON, writes canonical per-change `review-report.json`, generates `review-report.md` from JSON, validates routing/read-back/parity, and preserves safe-evidence wording.
- Delivery: `auto-chain`, `stacked-to-main`, review budget 400 changed lines.
- Mode: Standard. Strict TDD is disabled and no executable test runner is available.

### Slice 3: `shared-spec-compatibility`

- Boundary: Implement Phase 3 and remaining validation evidence only: update base `sdd-review-workflow` spec, shared persistence contract, and post-apply gates for canonical review JSON plus derived Markdown compatibility. Complete Phase 4 validation evidence as applicable. Do not add Excel/Python/script/workbook generation.
- Delivery: `auto-chain`, `stacked-to-main`, review budget 400 changed lines.
- Mode: Standard. Strict TDD is disabled and no executable test runner is available.

### Gate Fix 1: `apply-gate-blocker-fix`

- Boundary: Fix final apply gate blockers only: backend-neutral derived Markdown refs in the review-report schema, downstream phase skill wording for canonical JSON authority with derived Markdown compatibility, and README workflow documentation.
- Delivery: `auto-chain`, `stacked-to-main`, review budget 400 changed lines.
- Mode: Standard. Strict TDD is disabled and no executable test runner is available.

## Completed Tasks

- [x] 1.1 Created `skills/sdd-review/references/review-control-catalog.json` with canonical catalog metadata, vocabulary, presentation, validation, and all 96 `REV-CORP-001..REV-CORP-096` controls.
- [x] 1.2 Created `skills/sdd-review/references/review-report.schema.json` defining required per-change `review-report.json` fields, 96-row matrix shape, validation metadata, and derived Markdown references.
- [x] 1.3 Marked `skills/sdd-review/references/control-catalog.md` as derived/reference-only, with JSON winning on conflict and Markdown as the repair target.
- [x] 2.1 Updated `skills/sdd-review/SKILL.md` to load controls only from `review-control-catalog.json`, write canonical `review-report.json`, and render `review-report.md` from JSON.
- [x] 2.2 Added `sdd-review` validation/routing rules for catalog count/IDs/source items, row uniqueness, vocabulary, N/A evidence, verdict routing, persistence/read-back, and Markdown parity failures.
- [x] 2.3 Updated `skills/sdd-review/references/report-template.md` as the derived Markdown presentation contract mapping JSON fields to existing sections and the exact matrix header.
- [x] 2.4 Preserved safe-evidence wording in skill/template/schema: no secrets, credentials, tokens, PAN, PII, raw logs, production IDs, generated bytes, or final-document-only values.
- [x] 3.1 Updated `openspec/specs/sdd-review-workflow/spec.md` so canonical JSON catalog/report authority, derived Markdown compatibility, JSON-over-Markdown conflict policy, downstream compatibility, and deferred Excel/Python/workbook generation are part of the base workflow spec.
- [x] 3.2 Updated `skills/_shared/persistence-contract.md` so the review report resolver exposes canonical `review-report.json` and derived Markdown refs across OpenSpec, Engram, hybrid, and none modes while preserving the stable Engram compatibility key `sdd/{change-name}/review`.
- [x] 3.3 Updated `skills/_shared/sdd-post-apply-gates.md` so review-security, verify, and archive consume canonical JSON authority when present, may read derived Markdown for compatibility, and do not duplicate/re-score the general 96-control matrix.
- [x] 4.1 Static/manual checks for TD-001..TD-007 were completed in earlier slice evidence and task state was reconciled.
- [x] 4.2 Performed static/manual checks for TD-008..TD-015 against report schema identity, routing/evidence fields, row-count/N/A contracts, required Markdown sections, parity, and stale Markdown blocking.
- [x] 4.3 Performed static/manual checks for TD-016..TD-024 against routing, warnings, downstream compatibility, safe evidence, unavailable tooling, schema rejection rules, JSON-derived template, presentation metadata, and no Excel/Python/script/workbook additions.
- [x] 4.4 Recorded unavailable tooling from `openspec/config.yaml#testing`; no test, build, lint, type-check, format, coverage, Python, Excel, script, or workbook commands were invented or run.
- [x] Gate fix 1 corrected final apply gate blockers: `derivedMarkdownRef` no longer uses an OpenSpec-only pattern, downstream phase skills recognize canonical review JSON authority when present while preserving Markdown compatibility, and README documents canonical JSON plus derived Markdown refs.

## Files Changed

| File | Action | What Was Done |
| --- | --- | --- |
| `skills/sdd-review/references/review-control-catalog.json` | Created | Added canonical static 96-control catalog with metadata, vocabulary, presentation, validation, safe-evidence vocabulary, and no Excel/Python/script/workbook generation. |
| `skills/sdd-review/references/review-report.schema.json` | Created/Modified | Added JSON Schema-style contract for canonical per-change `review-report.json`; Phase 2 added safe-evidence and no Excel/Python/script/workbook wording. |
| `skills/sdd-review/references/control-catalog.md` | Modified | Added derived/reference-only authority notice and conflict policy: JSON wins; repair Markdown view on drift. |
| `skills/sdd-review/SKILL.md` | Modified | Changed the review contract to use canonical catalog/report JSON, derived Markdown rendering, catalog/report validation, read-back/parity blocking, safe-evidence constraints, and JSON+Markdown artifact reporting. |
| `skills/sdd-review/references/report-template.md` | Modified | Defined Markdown as a derived presentation from `review-report.json`, mapped JSON fields to required sections, preserved exact matrix header/96-row contract, and added parity/stale-Markdown blocking plus safe-evidence rules. |
| `openspec/specs/sdd-review-workflow/spec.md` | Modified | Updated base workflow requirements so canonical JSON owns general review controls/report facts, Markdown is derived compatibility, downstream phases recognize JSON authority, and Excel/Python/script/spreadsheet/workbook generation remains forbidden. |
| `skills/_shared/persistence-contract.md` | Modified | Added cross-mode review report refs for canonical JSON and derived Markdown, including OpenSpec paths, Engram topic semantics, hybrid expectations, none-mode inline behavior, and JSON-over-Markdown authority. |
| `skills/_shared/sdd-post-apply-gates.md` | Modified | Updated post-apply consumption rules so review-security, verify, and archive consume canonical JSON when present, may use Markdown for compatibility, and must not duplicate or re-score review matrices. |
| `openspec/changes/sdd-review-json-contract/tasks.md` | Modified | Marked Phase 1 tasks 1.1-1.3 and Phase 2 tasks 2.1-2.4 complete. |
| `openspec/changes/sdd-review-json-contract/tasks.md` | Modified | Marked Phase 3 tasks 3.1-3.3 and Phase 4 tasks 4.1-4.4 complete after static/manual evidence was recorded. |
| `openspec/changes/sdd-review-json-contract/apply-progress.md` | Modified | Merged slice 3 progress and validation evidence with existing slice 1 and slice 2 progress without dropping prior evidence. |
| `skills/sdd-review-security/SKILL.md` | Modified | Updated general review inputs and execution steps so canonical `review-report.json` is authoritative when present and derived Markdown is compatibility evidence only. |
| `skills/sdd-verify/SKILL.md` | Modified | Updated required inputs, review consumption, and output evidence wording to cite canonical JSON when present plus derived Markdown compatibility refs without re-scoring review matrices. |
| `skills/sdd-archive/SKILL.md` | Modified | Updated archive readiness/audit wording to preserve canonical general-review JSON when present plus derived Markdown compatibility refs and to treat JSON as authoritative on conflict. |
| `README.md` | Modified | Documented canonical review JSON plus derived Markdown compatibility refs for OpenSpec, Engram, and hybrid modes. |

## Deviations from Design

None — implementation matches the Phase 1, Phase 2, and Phase 3 design boundaries. Runtime execution of `sdd-review` itself is not performed in apply because the repository has no executable test runner and this change updates instruction/contract artifacts only.

## Static/Manual Test-Design Evidence

| Case | Status | Evidence |
| --- | --- | --- |
| TD-001 | Covered for current slices | `review-control-catalog.json` exists and declares `schemaName: sdd-review.control-catalog`, `status: canonical`; `sdd-review/SKILL.md` now states controls load only from this JSON and not from Markdown. |
| TD-002 | Covered | Static read-back found `expectedControlCount: 96`, 96 `controls[]` entries, 96 `REV-CORP-*` IDs, and source items `1..96`. |
| TD-003 | Covered | Catalog read-back shows required top-level fields: `schemaName`, `schemaVersion`, `catalogVersion`, `snapshotId`, `status`, `expectedControlCount`, `generatedHumanViewRef`, `vocabulary`, `presentation`, `validation`, and `controls[]`. |
| TD-004 | Covered | Catalog vocabulary and schema restrict compliance values to `Yes`, `No`, and `N/A`; `sdd-review/SKILL.md` blocks catalog/report vocabulary outside that set. |
| TD-005 | Covered | Catalog validation metadata lists required control fields; every control row includes `id`, `sourceItem`, `artifactDeliverable`, `requirement`, `reviewer`, `standard`, `severity`, `defaultComplies`, `evidenceHint`, and `notes`. `category` is optional JSON/presentation grouping metadata only. |
| TD-006 | Covered | Catalog/schema/template preserve the exact 10-column Markdown matrix and set `categoryColumnRendered: false`; `report-template.md` states the header must not add a Category column. |
| TD-007 | Covered | `control-catalog.md` is marked derived/reference-only; JSON wins on conflict and repair targets the Markdown view. |
| TD-008 | Covered for slice scope | `review-report.schema.json` requires report identity fields: `schemaName`, `schemaVersion`, `changeName`, `artifactKind`, `generatedAt`, `sourceCatalogRef`, `sourceCatalogSnapshotId`, and `derivedMarkdownRef`; `sdd-review/SKILL.md` requires canonical `review-report.json` with identity/catalog refs. |
| TD-009 | Covered for slice scope | `review-report.schema.json` requires `verdict`, `blockingFailureCount`, `nonBlockingFindingCount`, and `nextRecommended`; `sdd-review/SKILL.md` requires routing/counts to match matrix outcomes. |
| TD-010 | Covered for slice scope | `review-report.schema.json` requires `inputsInspected[]`, `runtimeChecks`, `blockingSummary[]`, `evidenceSummary`, `operationalEvidenceSummary`, `changedFileSecurityHandoff`, `validation`, and `presentation`; `report-template.md` maps these JSON fields to Markdown sections. |
| TD-011 | Contract covered; generated-report evidence deferred | `review-report.schema.json` constrains `reviewMatrix` to min/max 96 rows, and `sdd-review/SKILL.md` blocks generated JSON without exactly 96 unique rows mapped one-to-one to catalog controls. Actual per-change `review-report.json` generation belongs to the later `sdd-review` phase, not this apply slice. |
| TD-012 | Contract covered; generated-report evidence deferred | `sdd-review/SKILL.md` and `report-template.md` require every `N/A` row to include Evidence Location proving irrelevance and Observations/Comments explaining scope. Actual row evidence is produced during `sdd-review`. |
| TD-013 | Covered for slice scope | `report-template.md` preserves required Markdown sections: Verdict, Blocking Summary, Evidence Summary, Review Matrix, Operational Evidence Summary, Changed-File / Security Handoff, Matrix Validation, and Recommendation. |
| TD-014 | Covered for slice scope | `sdd-review/SKILL.md` and `report-template.md` require read-back parity: exact matrix header, 96 rendered rows, verdict/routing/counts, and key matrix facts compared to JSON. |
| TD-015 | Covered for slice scope | `sdd-review/SKILL.md` and `report-template.md` state Markdown generation, persistence, read-back, parity failure, or stale Markdown routes to `resolve-blockers` and must not be consumed downstream. |
| TD-016 | Covered | `sdd-review/SKILL.md` routes blocking/critical failed controls to `apply` and artifact/schema/context repair to `resolve-blockers`; `report-template.md` states critical/blocking `No` rows make verdict `FAIL` and route to `apply`; shared gates preserve `resolve-blockers` for artifact repair. |
| TD-017 | Covered | `sdd-review/SKILL.md` and `report-template.md` preserve non-blocking findings as warnings and only route to `review-security` when mandatory evidence is complete; `sdd-post-apply-gates.md` consumes non-blocking review evidence without hiding failed mandatory controls. |
| TD-018 | Covered | `persistence-contract.md` now resolves canonical `review-report.json` plus derived Markdown for OpenSpec, Engram, hybrid, and none; `sdd-post-apply-gates.md` lets review-security, verify, and archive read Markdown for compatibility while treating canonical JSON as authoritative when present. Downstream phases do not duplicate/re-score the 96-control matrix. |
| TD-019 | Covered for modified files | `sdd-review/SKILL.md`, `report-template.md`, and `review-report.schema.json` preserve safe-evidence wording forbidding secrets, credentials, tokens, connection strings, PAN, PII, raw logs, sensitive payloads, production IDs/production identifiers, generated bytes, and final-document-only values. |
| TD-020 | Covered | Static content/file review found no `.py`, `.xls`, `.xlsx`, `.xlsm`, or `.csv` files added. Modified contracts explicitly forbid Excel/Python/script/spreadsheet/workbook generation for this change. Existing repository PowerShell sync script references are unrelated pre-existing project utilities, not artifacts added by this slice. |
| TD-021 | Covered | `openspec/config.yaml#testing` records `test_runner.available: false`, empty test/build/coverage/lint/type-check/format commands, and no executable runner. Apply evidence records unavailable tooling instead of passing results. |
| TD-022 | Covered | `review-report.schema.json` uses `additionalProperties: false`, required fields for identity/routing/evidence/matrix/validation/presentation, consts for schema/catalog metadata, and min/max 96 matrix rows; evidence is static/manual because no schema validator command exists. |
| TD-023 | Covered | `report-template.md` maps Markdown sections to `review-report.json` fields and states Markdown is generated from JSON, never hand-authored as authority; `sdd-review/SKILL.md` requires JSON-first validation and rendering. |
| TD-024 | Covered | `review-control-catalog.json` includes `presentation.markdownTemplateRef`, exact `matrixColumns`, `categoryColumnRendered: false`, and `categoryUse` metadata sufficient for Markdown/future presentation grouping without adding Excel-specific generation. |
| Gate fix | Covered | `review-report.schema.json` accepts backend-appropriate derived Markdown refs (`openspec/changes/{change-name}/review-report.md`, `sdd/{change-name}/review`, or inline none-mode markers). `sdd-review-security`, `sdd-verify`, and `sdd-archive` now state canonical review JSON is authoritative when present and Markdown is compatibility evidence only. README documents the canonical JSON plus derived Markdown contract. |

## Security Evidence

| Guideline ID | Evidence |
| --- | --- |
| `SDD-SAFE-EVIDENCE` | Catalog vocabulary, schema, `sdd-review/SKILL.md`, and `report-template.md` use review-safe evidence concepts only; no secrets, credentials, tokens, connection strings, PAN, PII, raw logs, sensitive payloads, production IDs/production identifiers, generated bytes, or final-document-only values were added. |
| `SDD-FILE-SCOPE` | Edits are repository artifacts under allowed edit root only; no arbitrary filesystem access requirement was added. |
| `SDD-EVIDENCE-INTEGRITY` | JSON authority and derived Markdown conflict/parity policy are explicit: catalog JSON and report JSON are authoritative, Markdown is derived, and stale/parity-failed Markdown blocks downstream consumption. |
| `SDD-TOOLING-TRANSPARENCY` | `openspec/config.yaml#testing` records no executable test/build/lint/type/format/coverage tooling; no unavailable tool was reported as passing. |
| `SDD-SECURITY-OWNERSHIP-BOUNDARY` | This slice only defines general `sdd-review` 96-control JSON/Markdown contracts and does not modify `sdd-review-security` ownership or its 155-row source catalog. |
| `SDD-EVIDENCE-INTEGRITY` | Gate fix preserves JSON-over-Markdown authority across schema refs, review-security, verify, archive, and README while keeping Markdown as a derived compatibility artifact only. |

## Tooling Availability

- Test runner: unavailable (`openspec/config.yaml#testing.test_runner.available: false`).
- Build, coverage, linter, type checker, formatter: unavailable per `openspec/config.yaml#testing`.
- No test, build, lint, type-check, format, coverage, Python, Excel, script, or workbook command was run or added. Static/manual read-back used file reads and content search only.

## Remaining Tasks

None — all tasks in `openspec/changes/sdd-review-json-contract/tasks.md` are now checked complete.

## Status

Phase 1, Phase 2, Phase 3, Phase 4 validation evidence, and gate-fix evidence are complete. All implementation tasks are visibly checked in `openspec/changes/sdd-review-json-contract/tasks.md`; continuation should route to `review`.
